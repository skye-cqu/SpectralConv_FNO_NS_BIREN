# 2026 飞翔杯 · 全国智能科学探索挑战赛 —— 赛道五：模型与算子开发

> **Scientific Discovery Agent Challenge | Track 5: Models & Operators**
>
> 主办方：上海人工智能实验室（书生） & 壁仞科技（Biren Technology）
>
> 官网：https://ai4scompetition.intern-ai.org.cn/detail/operator

---

## 一、赛事简介

面向 AI for Science（AI4S）领域的核心计算算子与科学计算模型，鼓励参赛者依托壁仞 BIREN GPU 芯片及配套软件栈，采用 AI Skills/Agent 辅助开发模式，结合壁仞自研的模型/算子开发 Skills/Agent，完成高性能算子实现与科学计算模型部署，形成可运行、可验证、可复现的成果。本赛道设置 **1 道必选题**和 **3 道进阶题（三选一）**，参赛队伍须完成必选题，并从进阶题中任选一道完成。

---

## 二、赛道特色：Skills/Agent 驱动的国产 GPU 开发范式（必须项）

参赛者必须采用 AI Skills/Agent 辅助开发模式，结合壁仞自研模型/算子开发 Skills/Agent 与通用 AI 编程工具（如 Claude Code、Cursor 等），实践"人机协同"的高效开发范式。须记录 Skills/Agent 交互日志，展示如何利用 Skills/Agent 进行算子设计与调试、性能瓶颈分析、模型适配与优化、结果验证与迭代。**Skills/Agent 辅助开发记录为必须提交材料，未提交视为不合格。** Skills/Agent 辅助开发的深度、效率提升效果及工程实践质量将作为重要评审维度。

---

## 三、必选题：Spectral Convolution 算子开发

### 3.1 赛题简介

Fourier Neural Operator（FNO）是 AI for Science 领域最具代表性的神经算子架构之一，广泛应用于流体力学模拟、天气预报、地下水流预测等 PDE 求解场景。FNO 的核心创新在于 Spectral Convolution（谱卷积）算子——在傅里叶频域空间中学习全局卷积核，相比传统卷积具有分辨率无关性和全局感受野等优势。

计算流程：
```
输入特征 x(shape: [B, C_in, H, W])
→ FFT 变换到频域
→ 频域复数矩阵乘法（可学习权重 R, shape: [C_in, C_out, modes, modes]）
→ IFFT 变换回空间域
→ 输出特征 y(shape: [B, C_out, H, W])
```

> **注意**：FFT 运算建议优先支持空间维度为 2 的幂次（如 64, 128, 256）的输入 shape，以获得最佳计算性能。

### 3.2 任务要求

**基础要求（必须完成）：**
- 实现 2D Spectral Convolution 算子的前向计算（Forward）
- 支持输入 shape 为 [B, C_in, H, W]，输出 shape 为 [B, C_out, H, W]
- 支持可配置的傅里叶模态截断数 modes（仅保留低频分量）
- 首选在壁仞 BIREN GPU 芯片上完成运行
- 与 PyTorch 参考实现（CPU/CUDA）进行数值正确性对比，相对误差 ≤ 1e-4

**进阶要求（加分项）：**
- 实现反向传播（Backward），支持端到端训练
- 支持 3D Spectral Convolution（输入 shape [B, C, D, H, W]）
- 进行算子性能优化（显存优化、kernel 融合、计算并行度提升等）
- 提供不同分辨率（64×64, 128×128, 256×256）下的性能对比报告
- 展示 Agent 辅助开发过程（调试日志、性能分析对话记录）

### 3.3 评测标准

| 维度 | 权重 | 说明 |
|------|------|------|
| 正确性 | 35% | 与参考实现的数值一致性（相对误差 ≤ 1e-4） |
| 性能 | 25% | 考察算子执行时间、显存占用等；建议通过 SOL-Score (https://github.com/nvidia/sol-execbench) 评测算子实际表现与理论差距 |
| 代码质量 | 10% | 可读性、模块化、SUPA/extension 实现质量 |
| Agent 开发 | 15% | Agent 辅助开发的深度和效果（必须提交），考察算子开发过程、优化过程、性能评估与分析过程等交互文档以及 skills 沉淀；算子自动调优 skills 额外加分 |
| 扩展能力 | 15% | 进阶功能实现情况（3D、反向传播等） |

---

## 四、进阶题（三选一）

### 进阶题 A：Physics-Informed Neural Network 求解热传导方程

#### 4.1 赛题简介

Physics-Informed Neural Network（PINN）是将物理定律嵌入神经网络训练过程的经典方法，通过将 PDE 残差作为损失函数的一部分，实现无需标注数据即可求解微分方程。

求解目标：二维稳态热传导方程（Laplace/Poisson 方程）：∂²u/∂x² + ∂²u/∂y² = f(x, y), (x, y) ∈ Ω

应用场景：芯片散热分析、建筑热工设计、地热资源评估。

#### 4.2 任务要求

**基础要求：**
- 实现一个 MLP 网络（≥ 4 层，每层 ≥ 64 个神经元）作为 PDE 解的近似
- 构造 PINN 损失函数：PDE 残差损失 + 边界条件损失
- 在规则域（如单位正方形 [0,1]×[0,1]）上训练并求解
- 提供至少 1 个 benchmark 案例（如已知解析解的问题），验证相对 L2 误差 ≤ 1%
- 首选在壁仞 BIREN GPU 芯片上完成模型搭建与推理验证（训练为加分项，非硬性要求）

#### 4.3 评测标准

| 维度 | 权重 | 说明 |
|------|------|------|
| 模型搭建 | 30% | PDE 残差与边界损失正确；网络可用标准 Linear/激活；路径可在 BIREN 前向跑通；SUPA/extension 从优 |
| 正确性 | 30% | 相对 L2 ≤ 1%（可复现评估脚本）；仅前向给部分分 |
| 性能 | 10% | BIREN 单卡必报：主指标 points/s（评估网格前向），辅 grids/s、ms/grid、峰值显存 MB |
| 可视化 | 15% | 预测 / 真解 / 误差图清晰可读 |
| Agent 开发 | 15% | 必须提交；自动调优 skill 加分 |

**训练门槛：** 参考实现 n_epochs=10000（即 10000 step）。

| 档位 | step | 日志 | 正确性处理 |
|------|------|------|-----------|
| 冒烟 | < 1,000 | — | 流程分 |
| 有效（最低） | ≥ 3,000 | ≥10 次 loss（建议含 L2） | 可评实测 L2 |
| 推荐 | ≥ 10,000 | 每 ≤1,000 step 打点 | 利于达 L2 ≤ 1% |

---

### 进阶题 B：图神经网络预测分子性质

#### 4.4 赛题简介

分子性质预测是药物发现、材料设计、化学合成规划的基础任务。Graph Neural Network（GNN）通过将分子建模为图结构（原子为节点、化学键为边），学习分子的结构-性质映射关系。

应用场景：药物先导化合物筛选、新能源材料 HOMO-LUMO gap 预测、毒性预测。

#### 4.5 任务要求

**基础要求：**
- 实现 Message Passing Neural Network（MPNN）或 SchNet 风格的 GNN 模型
- 使用 QM9 数据集（约 13 万个小分子，≤ 9 个重原子）
- 预测至少 1 个分子性质（推荐 HOMO-LUMO gap 或 μ 偶极矩）
- 在标准数据集划分下，报告 MAE（Mean Absolute Error）
- 首选在壁仞 BIREN GPU 芯片上完成模型搭建与推理验证（训练为加分项，非硬性要求）

#### 4.6 评测标准

| 维度 | 权重 | 说明 |
|------|------|------|
| 模型搭建 | 30% | MPNN/SchNet 结构完整；scatter 等关键路径 SUPA/extension 从优 |
| 精度 | 30% | 验证/测试 MAE（声明性质与划分）；仅随机权重推理给流程分 |
| 性能 | 10% | BIREN 单卡、QM9、batch_size=32 必报：主指标 atoms/s，辅 molecules/s、ms/batch、峰值显存 MB |
| 代码质量 | 15% | 依赖、划分、随机种子、评估与测速脚本可复现 |
| Agent 开发 | 15% | 必须提交；自动调优 skill 加分 |

**训练门槛：** bs=32 时约 3,400-3,500 step/epoch。

| 档位 | step | 日志 | 精度处理 |
|------|------|------|---------|
| 冒烟 | < 1,000 | — | 流程分 |
| 有效（最低） | ≥ 3,000（≈1 epoch） | ≥10 次 train loss + val MAE | 可评实测 MAE |
| 推荐 | ≥ 10,000 或 ≥1 完整 epoch | 每 epoch 或 ≤500 step 打点 | 从优 |

---

### 进阶题 C：神经算子求解 Navier-Stokes 方程

#### 4.7 赛题简介

Navier-Stokes 方程是描述流体运动的基本方程。Fourier Neural Operator（FNO）通过数据驱动方式学习 PDE 解算子映射，训练完成后可对新的初始条件实现近实时推理。

求解目标：给定 t=0 时刻的涡度场 ω₀，预测 t=T 时刻的涡度场 ω_T。

应用场景：飞行器气动设计、工业流体仿真加速、海洋环流预测。

#### 4.8 任务要求

**基础要求：**
- 基于必选题实现的 Spectral Convolution 算子，搭建完整 FNO 模型（≥ 4 层 Fourier Layer）
- 使用公开的 Navier-Stokes 2D 数据集（64×64 粘度数据集）
- 完成模型训练，报告相对 L2 误差
- 首选在壁仞 BIREN GPU 芯片上完成模型搭建与推理验证（训练为加分项，非硬性要求）
- 可视化预测流场与 ground truth 对比

#### 4.9 评测标准

| 维度 | 权重 | 说明 |
|------|------|------|
| 模型搭建 | 30% | FNO 结构完整；复用本队必选 SpectralConv 从优；等价自研须说明对应关系 |
| 精度 | 25% | 相对 L2（声明 64×64 数据与划分）；仅前向给部分分 |
| 性能 | 10% | BIREN 单卡、64×64 NS、batch_size=16 必报：主指标 grid_points/s，辅 samples/s、ms/sample、峰值显存 MB |
| 可视化 | 20% | 预测与 GT 对比，须可见误差空间分布 |
| Agent 开发 | 15% | 必须提交；自动调优 skill 加分 |

**训练门槛：** 参考实现 n_train=1000、bs=16 → ≈63 step/epoch；n_epochs=100 → ≈6,300 step。

| 档位 | step（或等价 epoch） | 日志 | 精度处理 |
|------|---------------------|------|---------|
| 冒烟 | < 500（≈8 epoch） | — | 流程分 |
| 有效（最低） | ≥ 2,000（≈30 epoch） | ≥10 次 train relative L2 | 可评实测 L2 |
| 推荐 | ≥ 6,000 或 ≥100 epoch | 每 ≤10 epoch 打点 | 对齐参考训练量，从优 |

---

## 五、统一要求

### 开发环境

- 硬件平台：首选壁仞 BIREN GPU 芯片
- 语言：推荐 Python（模型层）、C++/SUPA（算子 kernel 层）
- 框架：推荐基于壁仞 br_pytorch 生态，可使用 PyTorch 兼容接口
- 算子实现方式：建议使用 SUPA 源码实现核心算子，或通过 torch.extension 封装自定义算子

### 模型与算子实现要求

模型题（进阶题 A/B/C）不建议直接使用原生 PyTorch 算子拼接模型。参赛者应体现对底层计算逻辑和 BIREN GPU 架构的深入理解：

- **方式一（推荐）**：使用 SUPA 源码直接实现模型中的核心算子（如消息传递、注意力计算、PDE 残差计算等），通过算子串联构建完整模型前向/反向计算流程
- **方式二**：通过 torch.extension（C++/SUPA 扩展）封装自定义算子，注册为 PyTorch 可调用的 Function/Module，在 Python 层组装模型结构
- **必选题（算子题）必须使用 SUPA 或 torch.extension 实现核心计算 kernel**

评审时将重点考察：算子实现的正确性与性能、对 BIREN GPU 计算特性的利用程度、kernel 设计的合理性（如内存合并访问、并行度、shared memory 利用等）。

### Agent 开发要求（必须项）

- 参赛者必须使用壁仞自研模型/算子开发 Agent 或通用 AI 编程工具（如 Claude Code、Cursor 等）辅助开发，Agent 辅助开发记录作为必须提交材料
- 必须提交 Agent 交互日志或截图（至少 5 段有效交互记录），未提交 Agent 记录的作品视为不合格
- Agent 使用场景要求（至少覆盖以下 3 项）：
  1. 算子 kernel 设计/调试/优化
  2. 模型架构选型与超参搜索
  3. 数据预处理与特征工程
  4. 性能瓶颈分析与定位
  5. 结果分析与可视化代码生成
  6. BIREN GPU 平台适配问题排查
- Agent 辅助开发的深度、效率提升效果及工程实践质量将作为重要评审维度（占比 15%）

### 提交规范

**一、`skill.md` 文件（必须提交）**

**二、其他文件**
1. 项目源码
2. 完整依赖说明与编译/运行命令
3. 正确性验证脚本与验证结果
4. 性能测试脚本与性能报告
5. 运行日志或截图
6. Agent/Skill 开发日志：至少 5 段有效交互记录（缺失则无效）
7. 展示材料（PPT/视频/可视化图片）：可选，但建议提交

### 运行要求

- 本赛道以考察模型搭建能力为主，模型训练和推理不作为硬性要求（完成训练为加分项）
- 必须提交：可运行的模型前向计算代码，能在 BIREN GPU 上完成单次推理验证
- 不要求多卡训练或分布式运行，优先保证单卡可运行、可复现
- 如提交训练流程：建议控制在 2 小时以内（可提供预训练 checkpoint）
- 完整评测脚本建议控制在 30 分钟以内
- **风险提示**：赛题开发过程中，个别开发环境可能引起异常，导致环境丢失风险，请及时保存个人开发成果，推荐将环境搭建过程脚本化

---

## 六、各题目计算特征汇总

| 题目 | 核心计算 | BIREN 平台关键依赖 | 显存估算 | 难度评级 |
|------|----------|-------------------|----------|---------|
| 必选：SpectralConv | FFT + 复数 einsum + IFFT | cuFFT 等价库、cfloat 支持 | 2-8GB | 中等 |
| 进阶 A：PINN | MLP 前向 + 二阶 autograd | 完整 autograd、create_graph | <2GB | 中等偏低 |
| 进阶 B：GNN | scatter_add + MLP | 高效 scatter/gather、动态索引 | <1GB | 中等 |
| 进阶 C：FNO-NS | 多层 FFT + 复数乘 + Conv2d | cuFFT 等价、cfloat、大显存 | 3-10GB | 中等偏高 |

关键 API 依赖清单（BIREN 平台需支持）：
- `torch.fft.rfft2 / irfft2 / rfftn / irfftn` — FFT 操作族
- `torch.einsum` — 复数张量缩并
- `torch.autograd.grad(create_graph=True)` — 高阶自动微分
- `scatter_add_` — 稀疏聚合操作
- `nn.Conv2d, nn.Linear` — 标准层
- `F.gelu, nn.Tanh, nn.ReLU` — 激活函数
- `nn.InstanceNorm2d` — 归一化层
- `Adam, lr_scheduler` — 优化器和调度器

---

## 七、赛事资源

### 书生 Intern Discovery 平台
- **科研助手**：支持学术问答与深度思考、自动化研究流程、代码生成与优化
- **SCP 广场**：连接数据、工具、智能体、专业模型、湿实验室等科研要素；提供 1600+ 科学发现工具 API，跨学科标准化环境
- **Intern Agent**：实现"想法生成 → 方案设计 → 代码实现 → 实验验证"全周期流程

### 壁仞专属开发者资源包
| 资源 | 数量 | 说明 |
|------|------|------|
| **国产 GPU 云端开发环境** | 限 300 份 | 基于壁仞 BR GPU 的免费云开发环境，支持全周期代码调试、模型推理和项目部署 |
| **API Key** | 限 1000 份 | 可调用 miniMax 2.7 等进行 AI 辅助开发 |
| **开发者课程** | — | 赛前学习中心：https://br.momodel.cn/ |

---

## 八、FAQ

**Q1：必须使用壁仞 GPU 吗？**
A：是的，所有提交首选在壁仞 BIREN GPU 芯片上运行验证。开发阶段可先在其他平台调试。

**Q2：进阶题 C 可以不使用必选题的算子吗？**
A：可以使用其他方式实现 FNO 模型，但复用必选题算子将获得额外加分（体现工程衔接能力）。

**Q3：Agent 日志如何提交？**
A：Agent 开发日志为必须提交材料。可以是 AI 工具的对话导出文件、终端日志截图、或整理后的 Markdown 文档，需体现真实交互过程，至少包含 5 段有效交互记录。

**Q4：数据集需要自己收集吗？**
A：所有题目均提供公开数据集来源。组委会将提供数据集下载链接或预处理脚本。

**Q5：可以使用预训练模型吗？**
A：允许使用公开预训练权重作为初始化，但须注明来源。核心算子必须自主实现。

**Q6：模型题可以直接用原生 PyTorch 实现吗？**
A：不建议。模型题要求参赛者使用 SUPA 源码实现模型的算子串联，或通过 torch.extension 的形式构建自定义算子并组装模型。

**Q7：提交中 skill.md 是什么？**
A：每个提交必须包含 skill.md 文件，作为该作品的功能描述和调用入口。详情见赛题引导文档。

---

> *注：以上内容整理自比赛官方网站 (https://ai4scompetition.intern-ai.org.cn/detail/operator)。更详细的 PyTorch 参考实现代码（SpectralConv2d、PINN、MPNN、FNO2d 的完整可运行代码）见官网赛题详情。*